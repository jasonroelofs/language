False.setSlots(
  # The `do` message on False will always execute the `ifFalse` block.
  do: { |ifTrue: {}, ifFalse: {}|
    ifFalse()
  },

  # The NOT operator negates the current value using truthiness rules.
  "!": {
    true
  },

  toString: {
    "false"
  },
)
