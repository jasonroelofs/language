Null.addSlots(
  # Null is a falsey value and thus the `do` message will always run the `ifFalse` block.
  do: { |ifTrue: {}, ifFalse: {}|
    ifFalse()
  },

  # The NOT operator negates the current value using truthiness rules.
  "!": {
    true
  },

  toString: {
    "null"
  },
)
